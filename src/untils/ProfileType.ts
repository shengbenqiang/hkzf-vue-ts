export interface UserMenuType {
  id: number;
  name: string;
  iconfont: string;
  to?: string;
}

export interface LoginData {
  account: string;
  password: string;
}

export interface EmptyType {
  emptyImg: string;
}
